import { TaskHighlightDirective } from './task-highlight.directive';

describe('TaskHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
